global:
  scrape_interval: 15s

scrape_configs:
  - job_name: 'litellm'
    static_configs:
      - targets: ['litellm:4000']  # LiteLLM metrics endpoint
    metrics_path: /metrics
    
  - job_name: 'ollama'
    static_configs:
      - targets: ['ollama:11434']  # Ollama metrics (if available)
